# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'myPod' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
pod 'Alamofire', '~> 4.7'
    pod 'MBProgressHUD', '~> 1.1.0', :modular_headers => true
  # Pods for myPod

  target 'myPodTests' do
    # Pods for testing
  end

end
